.DS_Store
node_modules
# we actually want the dist folder for github to host our site
# edit: scratch that, I set up a github action that automatically takes care of this
# so now dist is redundant to commit
/dist


# local env files
.env.local
.env.*.local

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor directories and files
.idea
.vscode
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
